# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json

node_modules
dist
dist-ssr
*.local
*.local.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

/target/
recording_*
.crush/
.cursor/

# Playwright
playwright-report/
test-results/
blob-report/

.direnv
.envrc

# Nix build output
result

# Claude Code project guide — local-only, keeps the repo history human-authored
/CLAUDE.md

# Local-only workspace — machine scratch, model weights, personal overrides (never committed)
/local/

# Upstream reference clone + design handoff bundle (not part of the shipped app)
/.reference/
/.design_extract/

# Rust build output (Cargo manifest lives in src-tauri/)
/src-tauri/target/
**/target/
**/*.rs.bk

# Environment & secrets
.env
.env.*
!.env.example

# Large model weights (downloaded at runtime into app data, never committed)
*.gguf
