# Normalize line endings: store text as LF, check out LF on every platform.
# Keeps the repo consistent with the prettier `endOfLine: "lf"` setting.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.icns binary
*.woff2 binary
*.gguf binary
*.onnx binary
*.wav binary
